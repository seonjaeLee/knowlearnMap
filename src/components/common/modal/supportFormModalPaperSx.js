/** 고객센터 Form 작성 팝업(FAQ·공지·QnA) — BaseModal `maxWidth={false}` `fullWidth={false}` 와 함께 사용 */
export const supportFormModalPaperSx = {
  width: 520,
  maxWidth: 'calc(100vw - 48px)',
  minHeight: 'min(720px, calc(100vh - 48px))',
};
